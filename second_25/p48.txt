WITH "c" AS (
 SELECT DISTINCT "sell_date" , "product" FROM "Activities" 
)
SELECT "sell_date" , COUNT("sell_date") AS "num_sold" , STRING_AGG("product" , ',' ) WITHIN GROUP (ORDER BY "product") AS "products"
FROM "c"
GROUP BY "sell_date" ;